<h1>Create a Song</h1>

<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <br />
  
  <%= f.label :artist_name %>
  <%= f.text_field :artist_name %>
  <br />

  <%= f.label :genre_name %>
  <%= f.text_field :genre_id, list: "genres_autocomplete" %>
  <datalist id="genres_autocomplete">
    <% Genre.all.each do |genre|%>
      <option value="<%= genre.name %>">
    <% end %>
  </datalist>
  <br />

  <%= f.fields_for :notes, Note.new do |notes_attributes| %>
    
    <%= notes_attributes.label :content %>
    <%= notes_attributes.text_field :content %>

  <% end %>
  <%= f.fields_for :notes, Note.new do |notes_attributes| %>
    
    <%= notes_attributes.label :content %>
    <%= notes_attributes.text_field :content %>

  <% end %>
  <%# <input name="song[notes_attributes][]" id="song_notes_1"><br>
  <input name="song[notes_attributes][]" id="song_notes_2"><br>
  <input name="song[notes_attributes][]" id="song_notes_3"><br>
  <br /> %>
  

  <%= f.submit %>
<% end %>
